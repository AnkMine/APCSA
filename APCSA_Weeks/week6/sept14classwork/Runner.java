// main runner class
public class Runner {

    // main method
    public static void main() {

        // creates new instance of Radius class
        Radius radius = new Radius();

        // calls methods from Radius class and inputs parameters
        radius.printArea(10);
        radius.printCir(10);
        radius.printConeVol(10, 5);

    }
}